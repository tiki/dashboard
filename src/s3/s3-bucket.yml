  StaticSiteBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${HostName}-dashboard-site'
      WebsiteConfiguration:
        IndexDocument: index.html
        ErrorDocument: error.html
  